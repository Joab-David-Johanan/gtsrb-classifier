data:
  root: data/gtsrb/gtsrb
  image_size: 224
  num_classes: 43
  num_workers: 4

model:
  backbone: resnet50
  pretrained: true
  dropout: 0.3

training:
  epochs: 30
  batch_size: 64
  learning_rate: 0.001
  weight_decay: 0.0001
  mixed_precision: true
  grad_clip: 1.0
  seed: 42

paths:
  checkpoints: checkpoints/
  logs: logs/
